# Configuración de Variables de Entorno

Copia este archivo como `.env` y configura las siguientes variables:

```
# Google Cloud
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/credentials.json
PROJECT_ID=your-project-id

# API Keys
GEMINI_API_KEY=your-gemini-api-key

# Firebase/Firestore
FIREBASE_CONFIG={"apiKey": "...", "authDomain": "...", ...}

# Other configurations
DEBUG=true
LOG_LEVEL=info
```

## Instrucciones

1. Obtén las credenciales de tu proyecto de Google Cloud
2. Configura las API keys necesarias
3. No subas el archivo `.env` al repositorio (ya está en .gitignore)